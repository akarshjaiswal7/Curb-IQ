# CurbIQ core runtime (intentionally GDAL/torch-free for a lean disk footprint)
numpy>=1.26
pandas>=2.2
scipy>=1.13
scikit-learn>=1.4
lightgbm>=4.3
shapely>=2.0
h3>=4.0
pyarrow>=15.0
python-dateutil>=2.9

# API
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
jinja2>=3.1
orjson>=3.9
httpx>=0.27

# Optimization & vision
ortools>=9.10        # patrol-routing VRP (a greedy+2-opt fallback runs if absent)
pillow>=10.0         # image IO for the camera-ingestion pipeline
# onnxruntime>=1.17  # OPTIONAL: real YOLOv8 inference for live CCTV (pip install to enable)
